"""Live input from the Discobox: the camera, the fan and LEDs, and the test run
that drives them, plus a replay of a recorded folder that stands in for them.

Only live runs import this package's camera and serial modules, so folder mode
never needs vmbpy or pyserial.
"""
