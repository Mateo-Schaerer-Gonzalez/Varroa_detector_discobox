__pycache__/
ground_truth_30min/
.pytest_cache/
calibration_data/
server.log
server.pid
update.log

# what the app keeps: the frames of every recording, and the results of each
recordings/
results/
# the saved test-run settings
settings.txt
vimbax.config

# where earlier versions kept them; the app moves them to the folders above
output/
outputs/
uploads/
