name: discobox_env
channels:
  - conda-forge
  - defaults
dependencies:
  - python >=3.11
  - numpy>=1.24
  - opencv>=4.7
  - pandas>=2.0
  - matplotlib>=3.7
  - openpyxl>=3.1       # pandas needs it to write .xlsx
  - pyyaml>=6.0
  - fastapi>=0.110
  - uvicorn>=0.27
  - pytest>=8.0
  - pyserial>=3.5      # the Discobox fan and LEDs (live runs only)
  - pip
  - pip:
      - python-dotenv
# vmbpy, for the camera, comes with the Vimba X SDK: bash install_linux.sh <VimbaX SDK path>
